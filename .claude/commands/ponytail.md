---
description: Switch ponytail intensity (lite/full/ultra/off)
argument-hint: "[lite|full|ultra|off]"
---
Invoke the `ponytail` skill at **$ARGUMENTS** intensity (default `full` when empty). `off` / `normal` deactivates lazy mode. The skill defines the behavior — apply it from here on.
